<?xml version="1.0" encoding="utf-8"?>
<MotionScene
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <Transition
        app:duration="500"
        app:constraintSetStart="@id/bottom_sheet_preview_open_state"
        app:constraintSetEnd="@id/bottom_sheet_expanded_open_state"
        app:motionInterpolator="easeInOut">

        <KeyFrameSet>
            <KeyAttribute
                android:alpha="0"
                app:framePosition="10"
                app:motionTarget="@id/description_container"/>
            <KeyAttribute
                android:alpha="0"
                app:framePosition="70"
                app:motionTarget="@id/description_container"/>

            <KeyPosition
                app:motionTarget="@id/username"
                app:framePosition="30"
                app:keyPositionType="parentRelative"
                app:percentY="0.25"
                app:percentX="0.8"
                />
        </KeyFrameSet>

    </Transition>

    <ConstraintSet
        android:id="@+id/bottom_sheet_preview_open_state">

        <Constraint
            android:id="@id/avatar"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintTop_toBottomOf="@id/drag_icon"
            app:layout_constraintStart_toStartOf="parent">

            <CustomAttribute
                app:attributeName="round"
                app:customDimension="32dp"/>

        </Constraint>

        <Constraint
            android:id="@id/username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/avatar"
            app:layout_constraintStart_toEndOf="@id/avatar"
            android:layout_marginStart="20dp"
            android:layout_marginTop="0dp"/>

        <ConstraintOverride
            android:id="@id/description">

        </ConstraintOverride>

        <Constraint
            android:id="@id/description_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/username"
            app:layout_constraintStart_toStartOf="@id/username"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="8dp"
            android:alpha="1">

        </Constraint>

        <Constraint
            android:id="@id/add_friend_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/description_container"
            app:layout_constraintStart_toStartOf="@id/username"
            android:layout_marginTop="8dp" />
    </ConstraintSet>

    <ConstraintSet
        android:id="@+id/bottom_sheet_expanded_open_state">

        <Constraint
            android:id="@id/avatar"
            android:layout_width="0dp"
            android:layout_height="200dp"
            app:layout_constraintTop_toBottomOf="@id/drag_icon"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <CustomAttribute
                app:attributeName="round"
                app:customDimension="16dp"/>

        </Constraint>

        <Constraint
            android:id="@id/username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/avatar"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="0dp"
            android:layout_marginTop="16dp"/>

        <ConstraintOverride
            android:id="@id/description">

        </ConstraintOverride>

        <Constraint
            android:id="@id/description_container"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/username"
            app:layout_constraintBottom_toTopOf="@id/add_friend_button"
            app:layout_constraintStart_toStartOf="@id/username"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:alpha="1">

        </Constraint>

        <Constraint
            android:id="@id/add_friend_button"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp" />

    </ConstraintSet>

</MotionScene>