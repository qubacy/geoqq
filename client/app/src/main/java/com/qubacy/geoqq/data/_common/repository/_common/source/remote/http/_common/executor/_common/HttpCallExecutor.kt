package com.qubacy.geoqq.data._common.repository._common.source.remote.http._common.executor._common

interface HttpCallExecutor {
    companion object {
        const val TAG = "HttpCallExecutor"
    }
}